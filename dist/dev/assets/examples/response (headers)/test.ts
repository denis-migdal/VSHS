// @ts-ignore
import {test} from "../../../../tests_helper.ts";

test('Response (headers)',
     'http://localhost:8080/response (headers)',
     {
        status: 200,
        mime  : "foo"
     }
);